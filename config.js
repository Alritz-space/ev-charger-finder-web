window.EV_CHARGER_CONFIG = {
  // For local development, keep the demo backend running at this URL.
  // For a hosted web app, replace this with your hosted backend API URL.
  API_BASE_URL: "http://localhost:3000"
};
